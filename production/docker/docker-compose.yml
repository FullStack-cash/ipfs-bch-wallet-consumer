# Start the service with the command 'docker-compose up -d'

version: '3.9'

services:
  mongo-bch-consumer-or:
    image: mongo
    container_name: mongo-bch-consumer-or
    ports:
      - '5557:27017' # <host port>:<container port>
    volumes:
      - ../data/database:/data/db
    command: mongod --logpath=/dev/null # -- quiet
    restart: always

  ipfs-bch-consumer-or:
    image: ipfs/go-ipfs:v0.11.0
    container_name: ipfs-bch-consumer-or
    environment:
      MY_ENV_VAR: 'placeholder'
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '10'
    mem_limit: 2000mb
    ports:
      - 4003:4003
      - 172.17.0.1:5003:5003
    volumes:
      - ../data/go-ipfs/data:/data/ipfs
      - ../data/go-ipfs/export:/export
    # https://docs.docker.com/compose/compose-file/compose-file-v3/#command
    # https://github.com/ipfs/go-ipfs/blob/91c52657166bcf86f2476926e4fe56694dc26562/Dockerfile#L115
    command:
      [
        'daemon',
        '--migrate=true',
        '--agent-version-suffix=docker',
        '--enable-pubsub-experiment'
      ]
    restart: always

  bch-consumer-or:
    build: .
    container_name: bch-consumer-or
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '10'
    mem_limit: 1500mb
    links:
      - mongo-bch-consumer-or
      - ipfs-bch-consumer-or
    ports:
      - '5012:5012' # <host port>:<container port>
    volumes:
      - ../data/ipfsdata:/home/safeuser/ipfs-service-provider/.ipfsdata
    restart: always
